# workflow [no indentation reduired for naming workflow]
name: Hello

on:
workflow_dispatch:

jobs:
 # 1st job give identation
 say-hello:
#create an ephermal container with ubuntu running 
 runs-on: ubuntu-latest

# ephermal container will have steps to execute  hence name those steps
steps:
- name:Say hello to everyone
  run:echo "hello Dosto"






# should inside steps
